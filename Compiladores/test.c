<!DOCTYPE html>
<html>
<head>
<title>Page Title< / title>
< / head>
<body>
<div>
<h1>This is a Heading< / h1>
<p>This is a paragraph.< / p>


<%
string pruebaAuto;

pruebaAuto = "hola" += "hola2";
struct Books {
	char  title[50][100];
	char  author[50];
	int  subject[100];
	int   book_id;

} book;


char grade;
switch (grade) {
case 'v':
	int a = 0;
	
	break;
case 'B':
	int b = 0;
case 'C':
	int c = 0;
	break;
case 'D':
	int d = 0;
	break;
case 'F':
	int e = 0;
	break;
default:

}






void gotoxy(int a)
{
	
}

int choice;
switch (choice)
{
case 1:
	int player = 1;
	int comp = 0;
	break;
case 2:
	int player1 = 0;
	int comp3 = 1;
	break;
case 3:
	int comp2 = 1;
	break;
default:
	int a;
}
enum DAY            /* Defines an enumeration type    */
{
	saturday,       /* Names day and declares a       */
	sunday = 4,     /* variable named workday with    */
	monday,         /* that type                      */
	tuesday,
	wednesday,      /* wednesday is associated with 3 */
	thursday,
	friday
};


float KrazyFunction(int *parm1[2], bool &p1size, int bb)
{
	int ix; //declaring an integer variable//
	string y = "hola";
	for (ix = 0; ix<p1size; ix++) {
		if (parm1[ix] == bb)
			return parm1[ix];
	
	}
	
	return 0;
}
int s[9];
bool goku;
float a = KrazyFunction(&s, *goku, 2);
const int ptr = 0x23;

#include "prueba.h" 
int ix;
enum DAY2            /* Defines an enumeration type    */
{
	saturday,       /* Names day and declares a       */
	sunday = 0,     /* variable named workday with    */
	monday,         /* that type                      */
	tuesday,
	wednesday,      /* wednesday is associated with 3 */
	thursday,
	friday
};


while (10 >= 20) {
	//printf("value of a: %d\n", *a);
	// a++;
	const char x_const = 'x';
}
const char x_const = 'x';
int   someSize;
int   ix2;
string xl = "hola";
char hola = x_const;
bool c = false;


bool f = false;
int x = 0;
for (x = 0; x < 10; x++) {
	/* Keep in mind that the loop condition checks
	the conditional statement before it loops again.
	consequently, when x equals 10 the loop breaks.
	x is updated before the condition is checked. */
	// string y = "hola";  
	//  printf( "%d\n", x );
	bool f1 = true;

	f = false;
}

int myArray[10][2] = { 5, 3, 6, 5, 5, 5, 5, 5, 5, 5 },test[12][32];

for (string item : someList) {
	char item;
	int someSize1;
}
myArray[0] = myArray[0];
float v3;
v3 = 2.2;

if (someSize > 0){
	float v4;
	int x2;
}


int xy;
/* The loop goes while x < 10, and x increases by one every loop*/
for (x = 0; x < 10; x++) {
	/* Keep in mind that the loop condition checks
	the conditional statement before it loops again.
	consequently, when x equals 10 the loop breaks.
	x is updated before the condition is checked. */
	// string y = "hola";  
	//  printf( "%d\n", x );
	//  bool f = true;
}


char cha, nplname[20], ch, plname[20];

// c = ix+someSize;


%>
<div>
< / body>
< / html >>